# tls:
#   stores:
#     default:
#       defaultCertificate:
#         certFile: /certs/cert.pem
#         keyFile: /certs/key.pem

http:
  routers:
    ping_router:
      rule: Host(`traefik-__DOMAIN__`) && Path(`/ping`)
      service: ping@internal
      entryPoints:
        - websecure
      tls: true

    dashboard_router:
      rule: Host(`traefik-__DOMAIN__`)
      service: api@internal
      entryPoints:
        - web
        - websecure
      tls: true
